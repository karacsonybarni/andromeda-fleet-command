name: Visual QA capture

on:
  pull_request:
    paths:
      - "src/**"
      - "art/**"
      - "Main.tscn"
      - "project.godot"
      - "AndromedaFleetCommand.Game.csproj"
      - ".github/workflows/visual-qa.yml"

permissions:
  contents: read

jobs:
  capture:
    runs-on: ubuntu-latest
    env:
      GODOT_VERSION: "4.7"
      GODOT_STATUS: "stable"
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "8.0.x"
      - name: Download official Godot .NET editor
        shell: bash
        run: |
          set -euo pipefail
          base="https://github.com/godotengine/godot/releases/download/${GODOT_VERSION}-${GODOT_STATUS}"
          curl -fL "$base/Godot_v${GODOT_VERSION}-${GODOT_STATUS}_mono_linux_x86_64.zip" -o /tmp/godot.zip
          unzip -q /tmp/godot.zip -d /tmp/godot
          editor="$(find /tmp/godot -type f -name 'Godot_*mono_linux.x86_64' | head -1)"
          chmod +x "$editor"
          echo "GODOT_EDITOR=$editor" >> "$GITHUB_ENV"
      - name: Build and capture rendered screens
        shell: bash
        run: |
          set -euo pipefail
          dotnet run --project tests/AndromedaFleetCommand.Core.Tests
          dotnet build AndromedaFleetCommand.Game.csproj --configuration Release
          "$GODOT_EDITOR" --headless --editor --path . --quit-after 10
          dotnet build AndromedaFleetCommand.Game.csproj --configuration Debug
          ./scripts/multiplayer-smoke.sh "$GODOT_EDITOR" editor cooperative
          ./scripts/multiplayer-smoke.sh "$GODOT_EDITOR" editor versus
          mkdir -p visual-qa
          log="$(mktemp)"
          command -v xvfb-run
          HOME="$(mktemp -d)" AFC_VISUAL_QA_DIR="$PWD/visual-qa" LIBGL_ALWAYS_SOFTWARE=1 \
            timeout 45s xvfb-run --auto-servernum --server-args="-screen 0 1920x1080x24 -ac" \
            "$GODOT_EDITOR" --path . --resolution 1920x1080 -- --visual-qa 2>&1 | tee "$log"
          grep -q "AFC_VISUAL_QA_PASS captures=10" "$log"
          python3 scripts/verify-visual-captures.py visual-qa
      - uses: actions/upload-artifact@v4
        with:
          name: visual-qa-${{ github.event.pull_request.head.sha }}
          path: visual-qa/*.png
          if-no-files-found: error
